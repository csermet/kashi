{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/csermet/kashi/packages/schemas/processed-track.v1.schema.json",
  "title": "KashiProcessedTrackV1",
  "description": "Processed lyrics document for one track. All timings are integer milliseconds. Schema version 1 is additive-only: consumers MUST ignore unknown fields; breaking changes require schema_version 2. When sync is \"line\", line objects carry no `words` array at all.",
  "type": "object",
  "required": ["schema_version", "pipeline_version", "generated_at", "track", "sync", "alignment", "lines"],
  "properties": {
    "schema_version": { "const": 1 },
    "pipeline_version": {
      "type": "string",
      "description": "Semver of the processing pipeline that produced this document; key input for reprocessing decisions.",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "generated_at": { "type": "string", "format": "date-time" },
    "track": {
      "type": "object",
      "required": ["source", "title", "artist", "duration_ms"],
      "properties": {
        "source": {
          "type": "object",
          "required": ["type", "id"],
          "properties": {
            "type": { "enum": ["youtube", "plex", "upload"] },
            "id": { "type": "string", "minLength": 1 }
          }
        },
        "title": { "type": "string" },
        "artist": { "type": "string" },
        "album": { "type": "string" },
        "duration_ms": { "$ref": "#/$defs/ms" },
        "canonical_group": {
          "type": "string",
          "description": "normalize(artist)|normalize(title)|round5(duration_s). Discovery index ONLY — never a cache key (source variants align differently)."
        }
      }
    },
    "sync": {
      "enum": ["word", "line"],
      "description": "Granularity of timing data. \"line\": no per-word timings anywhere in `lines`. \"word\": at least one line carries `words`; a line whose word timings were rejected by server-side QA omits the array and renders as plain text (mixed documents)."
    },
    "alignment": {
      "type": "object",
      "required": ["method", "quality_score"],
      "properties": {
        "method": {
          "type": "string",
          "description": "e.g. \"ctc-forced-aligner/mms-300m\" or \"lrclib-passthrough\" for line-only documents."
        },
        "lyrics_source": { "type": "string" },
        "lyrics_source_id": { "type": "integer" },
        "vocals_separated": { "type": "boolean" },
        "quality_score": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Normalized alignment confidence. Clients should fall back to line rendering below ~0.2 \u2014 the level this score's calibration associates with WRONG lyrics. Higher gates measure badly: against ground truth the score ranks real accuracy at Spearman +0.24, so a 0.5 gate discards documents that are in fact accurate (measured 2026-08-12: one gated document had a real PCO@0.3 of 0.979) while passing genuinely bad ones. Line-level doubt is carried per line by `uncertain` instead."
        },
        "quality_basis": {
          "type": "string",
          "enum": ["ctc-probs", "anchors", "probs+anchors", "line-anchors", "human"],
          "description": "Which FORMULA produced quality_score (pipeline 2.5.0+; the last two added in 2.16.0). \"ctc-probs\": calibrated CTC probability ramp alone — no reference stamps were available. \"anchors\": line-anchor agreement on the windowed path. \"probs+anchors\": the ramp over surviving words, scaled by the fraction of referenced lines that survived QA. \"line-anchors\": line-anchor agreement on a document that has NO word timings at all (sync=\"line\"). \"human\": human word data consumed as-is (lyricsfile), fixed 1.0. In every case except \"human\", word-level precision is NOT measured — a 1.0 can still drift at word granularity. Absent on documents older than 2.5.0."
        },
        "speed_factor": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Playback-speed factor the audio was corrected by before alignment (nightcore workflow); 1.0 for unmodified audio."
        },
        "qa": {
          "type": "object",
          "description": "Line-QA repair provenance (Faz 5): counts of lines snapped/dropped/shifted/rederived, the compensated median offset and the number of sustain-trimmed word ends. `uncertain` counts lines the drift threshold flagged but the audio vouched for (2.19.0); `nudged` counts lines that drifted BELOW the flag threshold and were moved onto their anchor because the audio backed that position (2.25.0) — a nudged line was never in doubt as content, only as position; `response_shifted` counts parenthetical responses moved onto the answering voice using the vocal loudness contour (2.27.0) — the aligner tends to staple a response to the tail of its call, and only loudness separates them. Consumers judging timing trustworthiness (e.g. a publish gate) read this; absent on documents older than pipeline 2.3.0.",
          "required": ["flagged", "density_dropped", "adlib_shifted", "adlib_rederived", "offset_ms", "trimmed_ends"],
          "properties": {
            "flagged": { "type": "integer", "minimum": 0 },
            "density_dropped": { "type": "integer", "minimum": 0 },
            "adlib_shifted": { "type": "integer", "minimum": 0 },
            "adlib_rederived": { "type": "integer", "minimum": 0 },
            "offset_ms": { "type": "integer" },
            "trimmed_ends": { "type": "integer", "minimum": 0 },
            "nudged": { "type": "integer", "minimum": 0 },
            "response_shifted": { "type": "integer", "minimum": 0 }
          }
        }
      }
    },
    "lines": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["start_ms", "end_ms", "text"],
        "properties": {
          "start_ms": { "$ref": "#/$defs/ms" },
          "end_ms": { "$ref": "#/$defs/ms" },
          "text": { "type": "string" },
          "score": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Optional per-line alignment confidence (enables mixed word/line rendering)."
          },
          "adlib": {
            "type": "boolean",
            "description": "Line is entirely nonlexical vocalization (ooh/whoa/la ad-lib); clients may style it differently (Faz 4 aesthetics). Omitted when false."
          },
          "words_derived": {
            "type": "boolean",
            "description": "This line's word boundaries are SYNTHETIC (redistributed across the line span for sweep aesthetics), not aligner-measured. Presentation data only — never contribute them as measured timings. Omitted when false; only appears alongside `words`."
          },
          "uncertain": {
            "type": "boolean",
            "description": "The drift threshold flagged this line but the audio vouched for its words (Faz 8 B4): they were block-shifted onto the lrclib anchor rather than deleted. Presentation hint — a client may de-emphasise the line; the timings are still aligner-measured. Omitted when false; only appears alongside `words`."
          },
          "words": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "required": ["start_ms", "end_ms", "text"],
              "properties": {
                "start_ms": { "$ref": "#/$defs/ms" },
                "end_ms": { "$ref": "#/$defs/ms" },
                "text": { "type": "string" }
              }
            }
          }
        }
      }
    },
    "beats": {
      "type": "object",
      "required": ["bpm", "times_ms"],
      "properties": {
        "bpm": { "type": "number", "exclusiveMinimum": 0 },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
        "times_ms": { "type": "array", "items": { "$ref": "#/$defs/ms" } },
        "downbeat_indices": {
          "type": "array",
          "items": { "type": "integer", "minimum": 0 },
          "description": "Indices into times_ms."
        }
      }
    },
    "palette": {
      "type": "object",
      "properties": {
        "source": { "enum": ["album_art", "default"] },
        "primary": { "$ref": "#/$defs/color" },
        "secondary": { "$ref": "#/$defs/color" },
        "background": { "$ref": "#/$defs/color" },
        "text": { "$ref": "#/$defs/color" },
        "accent": { "$ref": "#/$defs/color" }
      }
    },
    "fx": {
      "type": "object",
      "required": ["lexicon", "engine"],
      "description": "Semantic effect tags (pipeline 2.6.0+, Faz 6). SPARSE: most words carry no tag. From 2.13.0 words[] is a SELECTED set rather than every candidate, bounded by a per-song cadence of roughly one effect per 9 s (12-24 for a normal track) — see the select field. Indices reference lines[]/words[] of THIS document. Clients without effect support ignore the whole block.",
      "properties": {
        "lexicon": { "type": "string", "description": "fx lexicon version, e.g. \"kashi-fx/1.0.0\"." },
        "engine": { "type": "string", "description": "\"keywords\" or \"keywords+<model>@<revision12>\" when the embedding layer also ran." },
        "select": { "type": "string", "description": "Selection plan that thinned words[] (pipeline 2.13.0+, e.g. \"density/1.0\"). ANY non-empty value means the server already chose which words fire — density, per-line quota, spacing, chorus and song cadence are all applied — so a client must render every entry as-is. ABSENT means words[] is a raw candidate list from an older pipeline, and the client must apply its own legacy rule (one winner per line). The specific value is provenance, like lexicon/engine." },
        "words": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["line", "word", "tag", "intensity"],
            "properties": {
              "line": { "type": "integer", "minimum": 0 },
              "word": { "type": "integer", "minimum": 0 },
              "tag": { "type": "string" },
              "intensity": { "type": "number", "minimum": 0, "maximum": 1 }
            }
          }
        },
        "lines": {
          "type": "array",
          "description": "Line-level THEME tags from the embedding layer (no trigger word identified — theme only, no word effect).",
          "items": {
            "type": "object",
            "required": ["line", "tag"],
            "properties": {
              "line": { "type": "integer", "minimum": 0 },
              "tag": { "type": "string" }
            }
          }
        }
      }
    },
    "energy": {
      "type": "object",
      "required": ["rate_hz", "values"],
      "description": "Track-normalized loudness envelope (pipeline 2.6.0+): 0-100 ints sampled at rate_hz on the PLAYED clock (same as beats). Drives intensity ramps client-side.",
      "properties": {
        "rate_hz": { "type": "integer", "minimum": 1 },
        "values": { "type": "array", "items": { "type": "integer", "minimum": 0, "maximum": 100 } }
      }
    },
    "sections": {
      "type": "array",
      "description": "Coarse song sections (pipeline 2.6.0+). type is an OPEN string: v1 emits only energy-derived \"high\" blocks (chorus proxy); real structure labels (verse/chorus/bridge) may join additively later.",
      "items": {
        "type": "object",
        "required": ["type", "start_ms", "end_ms"],
        "properties": {
          "type": { "type": "string" },
          "start_ms": { "$ref": "#/$defs/ms" },
          "end_ms": { "$ref": "#/$defs/ms" }
        }
      }
    }
  },
  "$defs": {
    "ms": { "type": "integer", "minimum": 0 },
    "color": { "type": "string", "pattern": "^#[0-9a-fA-F]{6}$" }
  }
}
